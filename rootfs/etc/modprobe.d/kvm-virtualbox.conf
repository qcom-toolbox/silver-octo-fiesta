# Let KVM (QEMU/virt-manager) and VirtualBox share the CPU's virtualisation
# support: KVM only switches it on while a KVM virtual machine runs.
# (Only run VMs of one of them at a time.)
options kvm enable_virt_at_load=0
