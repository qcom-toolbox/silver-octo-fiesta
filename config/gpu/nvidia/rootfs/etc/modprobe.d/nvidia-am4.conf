# Kernel modesetting + framebuffer: required for KWin/SDDM on Wayland.
options nvidia_drm modeset=1 fbdev=1
# Keep VRAM contents over suspend (see /etc/elogind/system-sleep/nvidia).
options nvidia NVreg_PreserveVideoMemoryAllocations=1 NVreg_TemporaryFilePath=/var/tmp
